// Kanonik SEO origin — sitemap, robots, canonical/hreflang, JSON-LD tek kaynağı.
// APEX (www YOK) — root layout metadataBase ile aynı host.
// ⚠️ Auth/e-posta AYRI bir origin kullanır (www'lu HUB_AUTH_ORIGIN, lib/supabase-browser);
//    onu buradan türetme — bunlar bilinçli iki farklı host.
export const SITE_URL = 'https://travelbeez.gr'
